const index_entry_point = document.createElement('div');
let reactJS_script = document.createElement('script');

index_entry_point.id = 'index';

index_entry_point.appendChild(reactJS_script);

document.querySelector("body").appendChild(index_entry_point);